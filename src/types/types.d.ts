export interface SideBarMenuItem {
  id: string;
  label: string;
  icon: string;
  url: string;
}

export interface SideBarMenuCard {
  id: string;
  displayName: string;
  title: string;
  photoUrl: string;
  url: string;
}